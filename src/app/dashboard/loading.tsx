export default function Loading() {
  // Navigation inside the app is instant (routes are static + prefetched).
  // If a route hasn't been prefetched yet, keep the current screen visible
  // instead of flashing a full-screen spinner.
  return null;
}
